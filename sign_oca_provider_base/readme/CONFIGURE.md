This module does not add functionality by itself, but provides a base
implementation of the provider interface to be used by other modules.

To implement a provider, you need to create a new module that depends on
this one and implements the required methods.
